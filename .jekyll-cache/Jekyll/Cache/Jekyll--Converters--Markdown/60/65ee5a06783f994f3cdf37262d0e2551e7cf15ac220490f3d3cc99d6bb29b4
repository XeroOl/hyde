I"Ú<div class="language-lua highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reset</span> <span class="p">{</span><span class="n">beat</span><span class="p">}</span>
<span class="n">reset</span> <span class="p">{</span><span class="n">beat</span><span class="p">,</span> <span class="n">len</span><span class="p">,</span> <span class="n">eas</span><span class="p">}</span>
<span class="n">reset</span> <span class="p">{</span><span class="n">beat</span><span class="p">,</span> <span class="n">exclude</span> <span class="o">=</span> <span class="p">{</span><span class="n">mod1</span><span class="p">,</span> <span class="n">mod2</span><span class="p">,</span> <span class="n">mod3</span><span class="p">,</span> <span class="o">...</span><span class="p">}}</span>
</code></pre></div></div>
<p>It resets all mods to their default value at the specified beat. If <code class="language-plaintext highlighter-rouge">exclude</code> is provided, it will not reset any of the mods in the list.</p>

<p>See the <a href="setdefault.md">setdefault</a> page for more information on how to change the default value of a mod.</p>

<p>Examples:</p>
<div class="language-lua highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- reset the mods at beat 128</span>
<span class="n">reset</span> <span class="p">{</span><span class="mi">128</span><span class="p">}</span>

<span class="c1">-- reset the mods at beat 192, but keep 'reverse' and 'alternate' on</span>
<span class="n">reset</span> <span class="p">{</span><span class="mi">128</span><span class="p">,</span> <span class="n">exclude</span> <span class="o">=</span> <span class="p">{</span><span class="s1">'reverse'</span><span class="p">,</span> <span class="s1">'alternate'</span><span class="p">}}</span>

<span class="c1">-- reset the mods at beat 256 with an ease, but keep 'spiralx' on</span>
<span class="n">reset</span> <span class="p">{</span><span class="mi">256</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">outExpo</span><span class="p">,</span> <span class="n">exclude</span> <span class="o">=</span> <span class="s1">'spiralx'</span><span class="p">}</span>
</code></pre></div></div>
:ET